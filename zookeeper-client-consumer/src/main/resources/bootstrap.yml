
spring:
  application:
    name: zookeeper-client-consumer
  cloud:
    zookeeper:

      connect-string: 127.0.0.1:2181
      discovery:
        root: spring-cloud-service
        enabled: true
        register: true